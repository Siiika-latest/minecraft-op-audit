---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'e5876cb9-b415-4c59-a111-624b34760d27'
  PropagateID: 'e5876cb9-b415-4c59-a111-624b34760d27'
  ReservedCode1: '17d79d86-c615-4e86-9b8e-dbdef83ee7fe'
  ReservedCode2: '17d79d86-c615-4e86-9b8e-dbdef83ee7fe'
---

# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.1] - 2026-09-20

### 变更：榜单改名与新增（应服务器玩家命名习惯）

- 「破坏方块榜」→ **力工破坏方块榜**；「放置方块榜」→ **力工放置方块榜**
- 新增 **力工天榜**：当日破坏 + 放置方块数之和的榜（数据来自同一批 stats 汇总，不重复采集）
- 「PVP 击杀榜」→ **内鬼榜**（玩家互杀；字段名仍为 `pvp`，接口向后兼容）
- 排行榜 6 → **7 张**，同一套百分位色阶

### 新增：玩家击杀记录可按玩家查询

- 之前查某玩家时看不到他作为凶手的击杀记录（死亡事件的击杀者刻意不写进 `target`，
  防止僵尸等生物名混进玩家榜，导致 killer 匹配缺失）。
  现在筛选条件补上「该玩家是击杀者」匹配：查 yuuki 能看到他杀过谁（含时间、被杀者、死因、
  游戏内死讯），事件带「他是凶手」标记
- 视角标记细化：死亡事件的死者视角由「他执行」改为 **「他死亡」**（死亡不是他执行的行为）
- 玩家板块的击杀记录过滤同步（前端）

### 文案

- 统计卡片「破坏 / 放置方块」→「力工 破坏/放置」，「击杀 生物 / 玩家」→「击杀 生物/内鬼」
- 玩家概览标签「PVP 击杀」→「内鬼击杀」

### 测试

- `tests/test_dashboard.py`：114 → **125 项**（力工天榜聚合、凶手/受害者视角筛选）

## [1.2.0] - 2026-09-20

### 真机部署发现并修复的两个坑（2026-09-20 AFoP 服务器实测）

1. **ASI 拼接陷阱导致整个采集端加载失败**：`ServerEvents.command(event => {...})`
   注册语句后没有分号，下一行的独立 IIFE `(function () {...})()` 被 JS 自动分号插入
   规则吞进调用链，变成「把注册函数的返回值 **null 当函数调用**」，报
   `TypeError: null is not a function, it is object`。三个特征使它极难定位：
   - KubeJS 把这类顶层错误**统一报到脚本最后一行**（不同行数的版本报不同的行号）
   - 注册本身已生效（错误发生前 command 监听器已注册，看似"部分正常"）
   - **本地 235 项测试全部发现不了**——KubeJS 脚本从未在 JS 引擎里跑过。
   修复：事件注册一律以分号结尾，独立 IIFE 一律以防御性分号 `;(function` 开头，
   并把这条写进脚本顶部的「语法红线」注释。教训：**改 KubeJS 脚本后至少做一次
   `new Function(source)` 语法级验证 + 真机 reload 验证**。
2. **`PlayerEvents.logged_in` / `logged_out` 在 KubeJS 2001.6.5-build.16
   （MC 1.20.1 / Forge）上不存在**：注册时报 `Unknown event`（typeof 探测返回的
   是动态存根 function，调用时才报 Unknown，探针会误判为可用）。相关处理器已
   移除；登录建快照由巡检天然覆盖（首次见到玩家即建快照），下线冲账 stats
   退化为延迟到下一个 5 分钟汇总周期，影响可忽略。

### 新增：玩家行为统计（破坏方块 / 放置方块 / 击杀生物 / 击杀玩家）

四类高频行为**逐条写日志会撑爆服务端日志**（刷怪塔每秒可能死几百只怪），因此全部在
KubeJS 内存中聚合计数，**每 5 分钟（6000 tick）或玩家下线时**汇总输出一条
`ev=stats`；挂机零行为的玩家不产生任何输出。

| 行为 | 采集方式 | 上报 |
|---|---|---|
| 破坏方块 | `BlockEvents.broken`（事件源是玩家才计数） | `broken` |
| 放置方块 | `BlockEvents.placed`（同上） | `placed` |
| 击杀生物 | `EntityEvents.death` 非玩家分支：击杀者是玩家则计数 | `mob` |
| 击杀玩家 | 死亡事件中击杀者也是玩家时计数 | `pvp` |

- 死亡事件的 JSON 新增 `kp` 标记（击杀者是玩家时为 1），看板据此**从既有死亡数据
  直接推导 PVP**，不依赖 stats 汇总、也不与之双计；旧数据无标记时按「击杀者名
  出现在已知玩家集合」兜底判断，生物名（Zombie 等）不会混进 PVP 榜。
- 看板排行榜从 2 张扩到 **6 张**：管理行为、死亡次数、破坏方块、放置方块、
  击杀生物、PVP 击杀，同一套百分位色阶。
- 玩家板块与统计卡片显示破坏 / 放置 / 击杀数值。

### 新增：游戏模式巡检兜底（防「切了模式却没记录」）

Forge 1.20.1 **没有**「玩家游戏模式变化」事件，KubeJS 6 也没有 —— 命令事件只能
捕获走命令系统的 `/gamemode`；整合包 GUI 一键切换、mod 直接调用 `setGameMode()`
等**非命令途径**完全不产生命令，此前会漏记（真机出现过实例）。

- 采集端每 5 秒（100 tick）巡检一次在线玩家的模式快照，变化即上报 `ev=gmpatrol`；
  登录时只建快照不告警，离线清理快照。
- 采集器把「刚被命令记录过的同一次切换」（同玩家、同目标模式、120 秒窗口内）
  去重，同一次切换不会记两条。
- `/gamemode 1`、`/gamemode c` 等数字 / 缩写参数统一归一化，与巡检口径一致；
  详情文案同步改为中文（创造 / 生存 / 冒险 / 旁观），巡检记录带「（巡检发现）」标记。
- 命令事件的执行者模式从二态（creative/survival）升级为四态，旁观与冒险不再被
  误标为 survival。

### 新增：三道数据完整性防线（修复历史漏记的可能来源）

1. **原版日志兜底**（`vanilla_fallback`，默认开）：解析原版日志的
   `<玩家> issued server command: /命令` 行。KubeJS 脚本失效 / 未加载的窗口期，
   原版日志仍留有一份玩家命令记录。同一次命令与 `[MCAUDIT]` 行按
   「执行者 + 命令 + ±1 秒」**跨源去重**，不会双记。
2. **轮转归档补读**（`rotated_archive_scan`，默认开）：检测到日志轮转时，补读
   `logs/` 下未处理过的 `*.log.gz`。采集器停机恰逢轮转时，旧文件尾部未读的行
   不会再永久丢失。
3. **去重窗口持久化**：原始行去重从「进程内存」升级为 **crc32 稳定哈希 +
   state.json 持久化**（进程重启、归档补读都不重复入库；旧的 `hash()` 有
   PYTHONHASHSEED 随机化问题，跨进程不可用）。

### 修复：玩家板块点击玩家名无反应（UI）

根因：`renderView()` 在 `innerHTML` 渲染**之前**就绑定了 `.pitem` 的点击事件，
绑定目标是旧 DOM，渲染后监听全部随节点销毁失效 —— 只能用顶部下拉框筛选。

- 改为**事件委托**挂在不会被重建的容器上，一次绑定永久生效。
- 顺势把**时间轴、玩家板块、排行榜里的玩家名都做成可点击**（点击即筛选该玩家，
  死亡事件的击杀者仅玩家可点、生物名保持纯文本），并新增「全部玩家（不筛选）」
  列表项与「清除玩家筛选」入口。

### 修复与优化（看板）

- **下载 JSONL「今日」指向错误文件**：`toISOString()` 是 UTC 日期，北京时间 0–8 点
  会指向前一天（404）。改为按北京日期取文件。
- **自动刷新不再打断阅读**：30 秒轮询发现内容未变化时跳过重渲染，已展开的
  「原始日志」与滚动位置不再被折叠 / 跳动。
- **类型筛选芯片补全**：此前缺 `loot` / `xp` / `op` / `summon`，选中任一芯片时这些
  类型会被隐式过滤掉；现补齐并新增 `stats`。
- 「（未匹配到 ID）」提示只对 give / loot / item_set 显示 —— 附魔、药水事件本来
  就没有物品 ID 概念，此前属误导。
- 时间轴日期分组、CSV 下载等输出补充转义，堵住异常数据下的注入面。
- 统计卡片新增「破坏 / 放置方块」「击杀 生物 / 玩家」。

### 修复与优化（采集器）

- **配置文件兼容 BOM**：`config.json` 与台账 jsonl 改用 `utf-8-sig` 读取 ——
  Windows 记事本 / PowerShell 5.1 编辑过的文件带 BOM，此前会静默解析失败
  退回默认配置（或跳过一行事件）。
- `watcher.log` 超过 5 MB 自动截断保留尾部 200 KB；`state.json` 仅在有变化时写盘。
- 删除从未被使用的死代码 `RE_ADMIN_TAIL`；`RE_ISSUED` 由预留转为实际生效。
- 新增 `trim_watch_log` / `scan_rotated_archives` / `ingest_lines` / `merge_batch`
  / `parse_issued_line` / `GmPatrolFilter` / `norm_mode` 等独立函数，主循环瘦身为
  编排层，核心逻辑全部可单测。

### 修复（部署）

- `deploy/install.sh` 生成的 `enabled_types` **漏了 `death`**（v1.1.0 引入死亡统计时
  未同步），新装的服务器会因此漏记全部玩家死亡。已补齐 `death` 与 `stats`。

### 测试

- `tests/test_watcher.py`：63 → **121 项**（stats / gmpatrol 解析、模式归一化、
  巡检去重、跨源去重、原版兜底行、持久化去重、gz 归档补读整链路）。
- `tests/test_dashboard.py`：83 → **114 项**（行为统计聚合、PVP 双口径推导、
  四张新榜、known_player_names / killer_player 单元行为）。
- 浏览器冒烟测试（CDP 真实点击）：玩家列表项 / 时间轴玩家名点击筛选、清除筛选、
  排行榜六卡渲染，全部通过。

## [1.1.1] - 2026-09-17

### 修复：死因与击杀者在真机上会静默变成空字符串

1.1.0 照直觉写的三个取值方法，在 Minecraft 1.20.1 + Forge + KubeJS 6（Rhino）
真机上**全部不可用**；因为外面套了 `try/catch`，失败被静默吞掉 ——
看板照常运行、死亡次数照常统计，只是每条死亡都变成"死因：未知"、击杀者空白。
这类「看起来正常、实际全错」的失败最难发现，已在真机上逐个 dump 后改正：

| 原写法 | 真机结果 | 改为 |
|---|---|---|
| `DamageSource.getMsgId()` | `TypeError: Cannot find function getMsgId` | `DamageSource.type().msgId()`，兜底从 `String(ds)` 抠 `DamageSource (lava)` |
| `DamageSource.getEntity()` / `.getDirectEntity()` | 方法不存在 | `DamageSource.getActual()`，`getImmediate()` 兜底 |
| `DamageSource.getLocalizedDeathMessage()` | `InternalError: Can't find method ...DamageSource.m_6157_()` | `entity.getCombatTracker().getDeathMessage()` |

- `kubejs/admin_audit.js` 顶部注释补充了这四条兼容性结论（`V`/`X` 标注），
  并在死因、击杀者两处写明"为什么不能用直觉写法"。

### 修复：死因 id 用错命名法，原版死因大面积漏译

`DamageType.msgId()` 返回的是**驼峰短名**，与 `/damage` 里写的注册名并不一致。
1.1.0 的 `CAUSE_CN` 全部按注册名（蛇形）写，导致以下死因查不到中文：

| 注册名 | 实际 `msgId()` |
|---|---|
| `player_attack` | `player` |
| `mob_attack` | `mob` |
| `in_wall` | `inWall` |
| `out_of_world` | `outOfWorld` |
| `lightning_bolt` | `lightningBolt` |
| `hot_floor` | `hotFloor` |
| `generic_kill` | `genericKill` |

- 新增 `cause_key()`：查表前把驼峰转成蛇形，两种写法命中同一张表。
- `CAUSE_CN` 以实测 id 为准重写，保留 `mob_attack` / `player_attack` 等同义别名。
- 事件新增 `cause_cn` 字段（`cause` 仍保留原始 msgId），看板死亡卡片直接显示中文死因标签。

### 测试

- `tests/test_watcher.py`：52 → **63 项**。新增的一组用例直接标注了
  「2026-09-17 真机实测 dump」来源，覆盖 `player` / `mob` 与五个驼峰 id，
  防止有人"顺手改回"注册名写法。

## [1.1.0] - 2026-09-17

### 新增：玩家死亡统计

- **采集端**：新增 `EntityEvents.death` 监听，只上报**玩家**死亡，非玩家实体直接返回
  （`EntityEvents.death` 对所有生物触发，刷怪塔每秒可能死几百只怪，因此判断放在最前面）。
- **结构化死因**：不解析日志里的本地化死讯文本（可能是中文 / 英文 / 被模组改写），
  改用与语言无关的死因 id（详见 1.1.1 对取值方法的更正），例如 `fall` / `player` / `mob`；
  击杀者取 `DamageSource.getActual()`（射箭的箭会正确回溯到射手）；
  另存 `CombatTracker.getDeathMessage()` 的游戏内死讯原文，供对照。
- **死因中文化**：`collector/audit_watcher.py` 新增 `CAUSE_CN` 字典（50 余条），
  未收录的模组伤害类型原样显示，不丢信息。
- 采集器解析事件新增 `ev` 字段用于区分事件种类（`cmd` / `death`）。
  **不带 `ev` 的旧日志行仍按命令解析**，向后兼容。
- 日志格式新增一行形态：
  `[MCAUDIT] {"ev":"death","actor":"..","cause":"fall","killer":"Zombie","msg":".."}`

### 新增：每日排行榜（百分位配色参考 FF14 Logs）

- 看板新增**排行榜**视图，按天出两张榜：**管理员行为榜**与**死亡次数榜**，
  可切换统计日期（默认最新一天）。
- 百分位 = `100 × 当天「次数 ≤ 你的玩家数」 ÷ 当天上榜玩家数`，
  即「你胜过或战平了多少比例的人」。**当天最高的玩家必定是 100**，并列者同色。
- 色阶：`100` 金色 · `99` 粉色 · `91–98` 橙色 · `76–90` 紫色 · `51–75` 蓝色 ·
  `26–50` 绿色 · `1–25` 灰色。浅色主题用同色系深色版，深色主题用 FF14 Logs 原味配色。
- 次数为 0 的玩家不上榜；`console` / `@选择器` 等非玩家来源不进榜。
- 玩家板块新增「死亡 N 次」与「该玩家当日榜单百分位」徽章；统计卡片新增「玩家死亡」。

### 变更

- 玩家聚合新增 `deaths` 计数，**死亡不并入「作为执行者」**（死亡不是他做的行为）。
- 死亡事件的击杀者**刻意不写入 `target`**，否则僵尸、骷髅会被当成玩家混进玩家榜。
- CSV 台账的「命令原文」列对死亡事件留空（死亡没有命令，原先会写成一个孤零零的 `/`）。

### 测试

- `tests/test_watcher.py`：32 → **52 项**（新增死亡事件解析、死因中文化、
  击杀者不得污染玩家榜、新旧日志格式兼容）。
- `tests/test_dashboard.py`：39 → **83 项**（新增排行榜百分位、并列同色、
  色阶边界、控制台不上榜、零次不上榜、日期切换、筛选不影响榜单）。

## [1.0.0] - 2026-09-17

首个公开版本。

### 功能

- **采集端**（`kubejs/admin_audit.js`）：监听 `ServerEvents.command`，把每条命令写成一行
  `[MCAUDIT] {"ms","actor","cmd","gm"}` 日志。纯只读，不改动服务端本体。
- **采集器**（`collector/audit_watcher.py`）：跟随服务端日志，解析 11 类管理命令，
  补齐物品中英文名，输出 `events.csv` + `events-YYYY-MM-DD.jsonl`。
  支持日志 inode 轮转与文件截断，跨重启保持读取位置。
- **看板**（`dashboard/`）：纯标准库 HTTP 服务，两个视图 ——
  时间轴（按日期分组）与玩家板块（按玩家查阅其全部行为）。
  支持天数 / 玩家 / 类型 / 关键词筛选，台账下载（CSV、JSONL）。
- **一键部署**（`deploy/install.sh`）：自动探测服务端目录与日志、生成配置与随机令牌、
  构建物品名映射表、注册 systemd 服务、放行端口、自检并打印访问地址。
  幂等，可重复执行。
- **卸载**（`deploy/uninstall.sh`）：停止服务、清理防火墙规则，默认保留台账并备份。

### 兼容性

- Minecraft 1.20.1 + Forge 47.4.16 + KubeJS 6（`kubejs-forge-2001.6.5-build.16`）实测通过。
- 需要 Python 3.9+（标准库 `zoneinfo`），无第三方依赖。

### 记录到的兼容性陷阱

开发过程中实测确认的两个 KubeJS 6 行为（已写入脚本注释，避免后续踩坑）：

- `java()` 全局函数已移除，调用会抛错，**且该错误会逃逸脚本内的 `try/catch`**。
  取时间必须用 JS 原生的 `new Date()`。
- `player.getScoreboardName()` 在脚本中不可见。用它取玩家名会抛错，
  抛错被兜底逻辑吞掉后，**玩家行为会被静默误标为 `console`** ——
  功能看起来正常，实际全错。改用 `getGameProfile().getName()` 并加三层兜底。

### 修复

- 玩家聚合中，同一条事件里执行者与对象为同一人时（如 `/give Alice ... @s`），
  记录数、取物数、模式切换数会被重复计两次。现在每个玩家在同一条事件中只累计一次。

> AI生成